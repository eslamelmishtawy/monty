#include "monty.h"


int main(int argc, char **argv)
{
	stack_t stack = NULL;
	FILE *fd = NULL;
	char *line = NULL;

	if(argc != 2)
	{
		fprintf(stderr, "USAGE: monty file\n");
		exit(EXIT_FALIURE);
	}

}
